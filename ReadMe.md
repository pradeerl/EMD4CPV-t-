Install all dependencies with pip install -r requirements.txt.
Alter dataset sizes in utils.py by changing the size of n_iter, N_evt, x_plus, x_minus.
Create datasets for null hypothesis by running ParallelizedNull.py. Can be accessed for plotting histograms and chi-squared fit from "list_of_emds" files. 
Plot histograms using Plot Null Hypothesis Test.ipynb
